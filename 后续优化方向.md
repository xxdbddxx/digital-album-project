## 后续优化方向
# 照片索引
1. 对照片分类管理标签，比如用户可以通过物体（比如小猫小狗等）、日期（某年某月）、等标签进行索引到对应的照片，比如照一张去年夏天的照片，夏天即为标签。
或者参考以下内容检索（尽可能多给照片一些信息，后续用户方便通过与LLM对话照片内容）：
**优化**：引入向量数据库 (如 Milvus 或 Chroma)，将所有照片的背后故事、EXIF 时间、地理位置向量化。当用户问“上次我们在海边玩是什么时候”，AI 能直接从数据库检索并指令相框**主动跳转**到那组照片；或者当用户问“这是一张在哪拍的照片？”或“给我找出去看海的照片”时，LLM 能结合当前播放图片的元数据 (Metadata) 进行视觉问答，实现真正的**智能相册

### 当前进度
- [x] 第一阶段：统一设备照片索引，保存标签、拍摄日期、地点和来源。
- [x] 第一阶段：根据拍摄日期自动生成年份、月份和季节标签。
- [x] 第一阶段：设备照片库支持关键字、标签、日期范围和来源组合检索。
- [x] 第一阶段：网页端支持编辑照片文案、标签、拍摄地点和拍摄日期。
- [x] 第二阶段：使用 Gemini Embedding + SQLite 建立轻量照片向量索引。
- [x] 第二阶段：支持自然语言语义搜索，并可向设备下发目标照片跳转命令。
- [ ] 后续扩展：照片数量达到较大规模后，再评估迁移到 Chroma 或 Milvus。

# LLM对话
1. *降低语音唤醒或者Web界面下发指令 -> TTS合成人声播放的延迟*
2. 优化提示词，使得满足现有效果的基础山使得输出的JSON准确（更”智能“）

比如说“播放音乐”，当前提示词可能直接播放音乐，而不是继续询问“播放什么类型的/那首音乐？”或者ASR错误识别了一句话，LLM反而输出情绪为xx，不合时宜的打开香薰/音乐


# ui界面
优化LCD屏幕的显示效果即web下发新照片时的切换效果、LLM对话的效果（引入动态音频波形图，并在识别和思考时实时渲染用户指令字幕与大模型的回复字幕）

（以下可后续考虑）
增加 网络延迟的图标显示；
**环境自适应 UI**：UI 色调可与 AHT20（温湿度）与时间直接挂钩（如夜晚自动切深色模式，下雨天显示雨滴特效UI）。

# 网络配置
  **无感配网机制 (SmartConfig/SoftAP)**：废弃 `menuconfig` 烧录时硬编码 Wi-Fi SSID 和 服务器 IP 的方式，改为由手机扫码/来动态配置相册网络，增强易用性。
  **蓝牙配网**

# 其他优化待定...

## 2026-06-21 已完成模块

### 语音链路

- [x] INMP441 麦克风采集与诊断。
- [x] WakeNet“你好小智”唤醒和串口强制唤醒。
- [x] faster-whisper `large-v3-turbo` CUDA 识别。
- [x] DeepSeek V4 Flash 语义分析。
- [x] 阿里云 CosyVoice TTS，Edge TTS / Piper TTS 兜底。
- [x] 用户停止说话约 1.2 秒后自动结束录音。
- [x] 连续对话根据上一轮真实语音 RMS 动态判断。
- [x] WebSocket 长连接、断线恢复和会话缓冲清理。

### 对话与设备控制

- [x] 一句话同时切换目标照片和横竖屏方向。
- [x] 等待连续对话时不阻塞照片控制命令。
- [x] 对话框先显示回复文字，再播放对应 TTS。
- [x] WebSocket 异常退出时关闭语音助手遮罩。
- [x] ASR 常见同音词由 LLM 结合整句语义处理。

### 音乐播放

- [x] MAX98357A 功放和完整 PCM 音乐流播放。
- [x] 默认音量 60%，默认不循环。
- [x] 通过文件名、歌曲标题和歌手元数据检索。
- [x] 支持指定歌曲和随机播放。
- [x] 音乐播放可被“你好小智”打断。
- [x] 修复音乐、照片 HTTP 和语音 WebSocket 的网络资源竞争。

### 工具与工程稳定性

- [x] 一键重启 Flask 和 Voice 后端。
- [x] 重启时关闭旧 PowerShell 窗口及其子进程。
- [x] 固定 ESP-IDF v6.0.1 环境和 COM11 烧录流程。
- [x] wrapper 强制单线程编译，规避 GCC 内部崩溃。
- [x] API 密钥仅保存在本地 `.env.local`。

## 下一阶段建议

- [ ] 实机调试 PCF8574 + HE30 三路香薰控制。
- [ ] 接入 AHT20 后恢复温湿度数据，并验证不影响语音 I2C/I2S 链路。
- [ ] 为语音、照片和音乐关键流程增加自动化回归测试。
- [ ] 优化对话框打字速度与长文本分页策略。
- [ ] 在设备内存允许的前提下减少应用分区占用。
